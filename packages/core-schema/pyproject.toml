[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tenant-shield-schema"
version = "0.1.0"
description = "Shared Pydantic schemas for the Tenant Shield platform — the single source of truth for RunSpec, TestResult, and auth models."
requires-python = ">=3.11"
license = { text = "Proprietary" }
authors = [{ name = "Tenant Shield" }]
dependencies = ["pydantic>=2.5.0"]

[project.optional-dependencies]
dev = ["pytest>=7.4", "pytest-cov>=4.0"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
tenant_shield_schema = ["py.typed"]
